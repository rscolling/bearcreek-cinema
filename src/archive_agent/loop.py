"""Main async scheduler.

The daemon's job loop runs discovery, the show-state aggregator, taste
profile updates, ranking, and librarian passes on a cadence defined in
config. Implemented incrementally from phase2 onward; this module is an
empty placeholder at scaffold time so imports resolve.
"""
