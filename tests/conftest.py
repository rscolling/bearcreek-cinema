"""Shared pytest fixtures.

Individual fixtures are added per-phase as tests land. This file exists
at scaffold time so pytest discovers the ``tests`` package and the
conftest loader runs cleanly.
"""
